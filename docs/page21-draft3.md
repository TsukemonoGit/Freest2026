# 21ページ目案：赤外線信号を解析する

## やりたいこと

「エアコンのリモコンは、エアコン本体にどのような信号を送っているのか？」

これを知ることで、Picoから赤外線を発してエアコンを操作できるようになる。
そのために、まずPicoで赤外線信号を受信し、記録し、比較する必要がある。
このページでは送信ではなく**観察と分析**に焦点を当てる。

---

## 準備：信号を受信するコード

赤外線受信モジュール（PL-IRM1838B）をPico 2 WHのGPIOに接続し、信号の立ち上がり・立ち下がりのタイミングを記録する。

このコードでは、赤外線受信モジュールに信号が届くたびに、その時刻を記録する。信号が届いてから2秒間待ち、記録した時刻の差（間隔）を一覧にして表示する、という処理を書く。

```python
from machine import Pin
import time

IR_PIN = 15
ir = Pin(IR_PIN, Pin.IN)

edges = []

def on_edge(pin):
    edges.append(time.ticks_us())

ir.irq(trigger=Pin.IRQ_FALLING | Pin.IRQ_RISING, handler=on_edge)

print("リモコンのボタンを1回だけ押してください")
edges.clear()
time.sleep(2)
ir.irq(handler=None)  # 記録を止める

# エッジ間隔（パルス幅）を計算
durations = []
for i in range(1, len(edges)):
    durations.append(time.ticks_diff(edges[i], edges[i - 1]))

print(f"エッジ数: {len(edges)}")
print(durations)
```

このコードを実行し、リモコンの「電源ON」ボタンを1回押すと、`durations`にマイクロ秒単位の数値の並びが表示される。
この数値の並びが、その信号の正体。

---

## いろんなボタンを押してみる

同じ手順で、複数のボタンについて信号を記録する。

- 電源ON
- 電源OFF
- 温度を1℃上げる
- 温度を1℃下げる
- 風量を変える
- 運転モードを変える

それぞれの`durations`をメモに控えておく。信号は長いため、全部を目で追う必要はない。まず**長さ（配列の要素数）**と**先頭付近の数値**を見比べる。

---

## 分析：パターンを比較する

多くのエアコンリモコンは、AEHAフォーマットと呼ばれる形式に準拠しているとされる（メーカーによって異なる可能性がある）。この形式では、先頭に長いヘッダパルスがあり、その後に信号の本体（データビット）が続く。

ビットへの変換は、スペース（パルスとパルスの間の無信号区間）の長さによって0か1かを判定する方法が使われることが多い。

このコードでは、記録した間隔の数値を1つずつ調べ、間隔がしきい値より長ければ1、短ければ0として、数値の並びを0と1の列に変換する処理を書く。

```python
def durations_to_bits(durations, threshold=800):
    # threshold（しきい値）は実測して調整する
    # 一般に、短いスペースは0、長いスペースは1に対応するとされる
    bits = []
    for i in range(2, len(durations), 2):  # ヘッダ部を除き、スペース長のみを見る
        space = durations[i]
        bits.append(1 if space > threshold else 0)
    return bits

bits = durations_to_bits(durations)
print(bits)
```

「電源ON」と「電源OFF」で記録した`durations`をこの関数にかけ、出力されたビット列を並べて比べる。

| ボタン | ビット列（例） |
|---|---|
| 電源ON | `[結果を貼る]` |
| 電源OFF | `[結果を貼る]` |
| 温度+1℃ | `[結果を貼る]` |
| 温度+2℃ | `[結果を貼る]` |
| 風量変更 | `[結果を貼る]` |

実際の値は、使用しているエアコンの機種によって異なる。

---

## 「へー」となる

ビット列を並べて見ると、いくつかの規則性に気づくことがある。

- 全体の**先頭部分は毎回ほぼ同じ**（機種やメーカーを識別するための固定部分である可能性がある）
- **温度を変えたときだけ変化する箇所**がある（温度を表すビット群と推測できる）
- **風量を変えたときだけ変化する箇所**がある（温度の変化とは別の位置である可能性が高い）
- 電源ON/OFFは、温度や風量の情報とは独立した、別のビットで表現されている可能性がある

ここまでの作業で、「見た目は謎の数値の羅列」だった信号が、「温度・風量・電源などの情報が、決まった位置のビットに対応している」という構造として見えてくる。

---

## では、自分で組み立ててみよう

信号の構造（どの位置のビットが何を意味するか）が分かれば、逆に「このビットをこう組み立てれば、任意の温度・風量を指定した信号を作れる」という発想に進める。

次のページでは、この解析結果をもとに、Picoから**送信する信号を自分で組み立てる**方法を扱う。
